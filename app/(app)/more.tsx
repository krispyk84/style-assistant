import { View } from 'react-native';

// Placeholder — this route exists for Expo Router's file-based routing.
// The tab is intercepted via tabPress listener in _layout.tsx and never navigates here.
export default function MoreScreen() {
  return <View />;
}
